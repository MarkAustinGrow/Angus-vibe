# Core dependencies (compatible versions)
python-dotenv>=1.0.0
httpx>=0.24.0
supabase>=1.0.3
openai>=1.1.0
requests>=2.28.0
schedule>=1.1.0

# Flask web framework
flask>=2.0.0
Werkzeug>=2.0.0

# Google API dependencies
google-auth>=2.16.0
google-auth-oauthlib>=0.8.0
google-auth-httplib2>=0.1.0
google-api-python-client>=2.80.0

# Audio/Video processing (Angus-specific)
pytube>=12.1.0
pydub>=0.25.1

# Utility libraries
uuid>=1.30
tblib>=1.7.0

# Security
cryptography>=40.0.0

# Testing
pytest>=7.3.1

# CrewAI integration (compatible versions)
crewai>=0.28.0
crewai-tools>=0.1.6

# LangChain ecosystem (compatible versions from Yona)
langchain>=0.1.0
langchain-openai>=0.0.5

# Coral Protocol dependencies (exact working versions)
langchain_mcp_adapters==0.0.11
aiohttp>=3.8.5
sseclient-py>=1.7.2

# Additional dependencies that may be needed
langchain-community>=0.1.0
langchain_experimental>=0.0.42
